Initial public release of Marimba, a Marine Imagery Batch Actions tool for structuring and processing scientific image datasets.

The release establishes the project layout, a Typer/Rich CLI skeleton, and early integration with the iFDO standard. Core dependencies include Pillow, OpenCV, `pyexiv2`, `scikit-learn`, `pandas`, and `czifile`. Python 3.8+ required.